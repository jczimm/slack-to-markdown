{
  "name": "slack-to-markdown",
  "version": "1.0.1",
  "description": "Convert Slack's clipboard format to Slack-compatible Markdown",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/slackToMarkdown.d.ts",
      "import": "./dist/slackToMarkdown.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "node --experimental-strip-types --test 'src/**/*.test.ts'",
    "build": "tsc"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cozmo/slack-to-markdown.git"
  },
  "keywords": [
    "slack",
    "markdown",
    "clipboard"
  ],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
